import { ValueProjectionData } from './value-projection-data';

describe('ValueProjectionData', () => {
  it('should create an instance', () => {
    expect(new ValueProjectionData()).toBeTruthy();
  });
});
